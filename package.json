{
  "name": "@21gifts/spend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Daily Lightning gift payouts: invoices from 21.gifts api, pay via lightning.space",
  "license": "UNLICENSED",
  "scripts": {
    "start": "bun src/server.ts",
    "cli": "bun src/cli.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "e2e": "playwright test",
    "e2e:visual": "playwright test e2e/visual.spec.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^22.10.2",
    "@vitest/coverage-v8": "^2.1.8",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "engines": {
    "bun": ">=1.3"
  },
  "dependencies": {
    "uqr": "^0.1.3"
  }
}
